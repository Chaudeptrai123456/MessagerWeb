server:
  port: 9999  # để Auth Server chạy cổng 8081 (khớp với FastAPI sẽ gọi)

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/message_db
    username: user
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update   # hoặc create-drop nếu muốn reset mỗi lần chạy
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: always

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 325019990890-qe5jj732uqji378c1ssv54ghh09dtoab.apps.googleusercontent.com
            client-secret: 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
            scope:
              - email
              - profile
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            authorization-grant-type: authorization_code
            client-name: Google

app:
  auth:
    host: localhost
    port: 9999

logging:
  level:
    org.springframework.security: DEBUG
